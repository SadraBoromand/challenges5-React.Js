import React from "react";
import classes from './replyButton.module.css';

const ReplyButton = () => {

  return (
    <div>
      <span className={classes.text}>Reply</span>
    </div>
  );
};

export default ReplyButton;
